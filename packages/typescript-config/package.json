{
  "name": "@react-pixel-ui/typescript-config",
  "version": "1.0.0",
  "description": "Shared TypeScript configuration for React Pixel UI",
  "main": "index.js",
  "files": [
    "base.json",
    "react-library.json",
    "react-app.json"
  ],
  "scripts": {
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}